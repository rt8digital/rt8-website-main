import React from 'react';
// Mock component to resolve TypeScript error
// The actual particlering.jsx file is missing, but we're providing a placeholder

const ParticleRingBackground: React.FC = () => {
  return null;
};

export default ParticleRingBackground;